object Example {
  def main(): Unit = {
    println(new B().foo());
  }
}

class B extends A {
  def foo(): Int = {
    value = 42;
    return value;
  }
  def another(): Int = {
    
    return 1;
  }
}

class A {
  var value: Int;
  var test : A;
  def foo(): Int = {
    var value: Bool;
    value = false;
    value = test.another();
    return 41;
  }
}