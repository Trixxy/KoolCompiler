/*
mycode.kool:13:9: Warning: Variable b is declared but never used.
    var b: Int;
         ^
*/

object Example {def main(): Unit = {
{{
  println(1+new A());
    if(true){
    }
  }}
}}

class A{
}


//          println(lhs.getType+" + "+rhs.getType+" = "+expr.getType)