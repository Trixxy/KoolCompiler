object B {
  def main(): Unit = {
    println(new B().foo());
  }
}

class C extends B {}

class B{
  var value: Int;
  var test : A;
  def foo(): Int = {
    var value: Bool;
    value = false;
    //value = test.another();
    return 41;
  }
}

class A {
  def foo(): Int = {
    //var value : Int;
    //var value : Int;
    value = 42;
    return value;
  }
  def fliepp(): Int = {
    return 1;
  }
  def another(): Int = {
    
    return 1;
  }
}

